import 'package:flutter/material.dart';

class TextEditorToggler extends StatelessWidget {
  const TextEditorToggler({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
