/// Type of notification, used to determine the styling and icon of the notification.
enum NotificationType { info, warning, error, success, loading }

/// Priority of the notification, which can be used to determine the order of display
enum NotificationPriority { low, medium, high, critical }
